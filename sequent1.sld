(library (sequent1)
  (export eva)
  (import (chezscheme))
  (include "match.scm")
  (include "sequent1.scm"))

;; (import (sequent1))

(include "match.scm")
(include "sequent1.scm")
;; (trace unify/trunk)
